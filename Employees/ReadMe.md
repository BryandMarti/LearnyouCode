<!-- To get all employees: GET http://localhost:3000/employees
To get a specific employee by ID (e.g., employeeID = 3): GET http://localhost:3000/employees/3
If you try to access an employee with an incorrect ID (e.g., employeeID = 11), it will return a 404 status code and an error message. -->